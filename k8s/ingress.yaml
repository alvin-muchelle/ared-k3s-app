apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: dummy-ingress
spec:
  ingressClassName: traefik
  tls:
    - hosts:
        - "traefik.local"
      secretName: dummy-tls
  rules:
    - host: "traefik.local"
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: dummy-service
                port:
                  number: 80
